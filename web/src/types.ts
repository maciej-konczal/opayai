export type Constraints = { max_total: number; currency: 'PLN'; categories: string[]; requires_refundability: boolean; min_return_window_days: number; deliver_by: string | null; allowed_rails: string[]; expiry: string }
export type Intent = { id: string; description: string; status: 'draft' | 'open' | 'fulfilled' | 'revoked' | 'expired'; constraints: Constraints; spent_total: number }
export type Offer = { sku: string; title: string; category: string; price: number; stock: number; delivery_method: string; delivery_estimate_days: number; return_policy: {window_days: number}; refundable: boolean; attributes: Record<string, unknown> }
export type Purchase = { id: string; intent_id: string; status: string; order_status: string; proposal: Record<string, unknown>; cart?: { id: string; totals: { total: number }; signing?: unknown }; payment?: { rail_ref: string; status: string; attempts: unknown[] }; order?: { id: string }; exception?: { type: string; evidence: { expected: unknown[]; observed: unknown[] }; resolution_status: string }; events?: LedgerEvent[] }
export type LedgerEvent = { seq: number; ts: string; actor: string; type: string; payload: Record<string, unknown>; hash: string }
