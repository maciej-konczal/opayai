# OPayAI contracts

These JSON documents are the shared boundary between `backend/` and `web/`.
They intentionally contain no implementation imports. Update a contract before
changing either side of the language boundary.

The MCP surface is intentionally proposal-only: no tool signs, approves, or
confirms a purchase or return.
